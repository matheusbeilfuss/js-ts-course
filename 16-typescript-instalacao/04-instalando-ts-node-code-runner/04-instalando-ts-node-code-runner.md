# Instalando o ts-node e Code Runner

- `npm i ts-node -D`
- Com a configuração dessa lib em `.vscode/settings.json`, qualquer arquivo TS pode ser executado com ts-node.
  - `--files` elimina os arquivos de saída de cada execução.
