# Sintaxe de Type Annotations no TypeScript

- Type Annotation: como falar para o TS qual o tipo de uma variável
- Letra minúscula para tipos (há casos em que é maiúscula)
- "any" é qualquer tipo, indica que o TS não conseguiu inferir o valor
